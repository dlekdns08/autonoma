[project]
name = "autonoma"
version = "0.1.0"
description = "Self-organizing autonomous agent swarm with animated terminal UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.92.0",
    "openai>=1.0.0",
    "pydantic>=2.10.0",
    "pydantic-settings>=2.7.0",
    "rich>=15.0.0",
    "click>=8.1.0",
    "aiofiles>=24.1.0",
    "fastapi>=0.135.3",
    "python-multipart>=0.0.20",
    "uvicorn>=0.44.0",
    "websockets>=16.0",
    "sqlalchemy>=2.0.30",
    "aiosqlite>=0.20.0",
    "greenlet>=3.0",
    "bcrypt>=4.0.0",
    "itsdangerous>=2.2.0",
]

# Optional heavy ML stack for OmniVoice zero-shot TTS.
#
# Kept out of the base install because torch + torchaudio + librosa +
# model weights add ~2GB to dev environments that never touch voice.
# Enable with:  uv sync --extra tts
# The Dockerfile.api stage adds this extra so the deployed API has
# synthesis; the CLI-only build can skip it.
[project.optional-dependencies]
tts = [
    "omnivoice>=0.1.4",
    "soundfile>=0.13",
    "numpy>=2.0",
    "torch>=2.1",
    "torchaudio>=2.1",
    # omnivoice imports librosa lazily via lazy_loader but doesn't
    # declare it in its own Requires-Dist, so uv won't pull it in.
    # Without it, the first /test call on a voice profile dies with
    # ``ModuleNotFoundError: No module named 'librosa.core'`` deep
    # inside the OmniVoice ref-audio loader.  Pin it explicitly here
    # so every fresh workspace resolves it.
    "librosa>=0.10",
]

# OmniVoice via MLX (Apple-Silicon-native inference). The mlx-* wheel
# matrix only ships macOS arm64 builds, so every dependency carries a
# platform marker — running ``uv sync --extra omnivoice-mlx`` on Linux
# / x86_64 resolves to an empty set rather than failing. Enable with:
#     uv sync --extra tts --extra omnivoice-mlx
# At runtime select via ``AUTONOMA_TTS_PROVIDER=omnivoice-mlx``.
omnivoice-mlx = [
    "mlx-audio>=0.4.3 ; sys_platform == 'darwin' and platform_machine == 'arm64'",
]

# Memory RAG (feature #5) — optional embedding backend for AgentMemory.
# Without this extra ``recall_semantic`` silently falls back to keyword
# substring search (the historical behaviour). Enable with:
#     uv sync --extra memory-rag
# Reuses the torch already pulled by ``tts`` if both extras are on.
memory-rag = [
    "sentence-transformers>=2.7",
]

# Microsoft VibeVoice intentionally omitted from optional-dependencies
# because uv struggles to resolve the upstream git source against our
# requires-python matrix. Operators install it imperatively in the
# venv that runs the API:
#
#   uv pip install \\
#     'vibevoice[streamingtts] @ git+https://github.com/microsoft/VibeVoice.git@main'
#
# At runtime select via ``AUTONOMA_TTS_PROVIDER=vibevoice``. The TTS
# factory falls back to StubTTSClient if the package isn't installed
# (loud warning in the log), so leaving this out doesn't break boot.

[project.scripts]
autonoma = "autonoma.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autonoma"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
# Default options: short tracebacks for CI logs, run quietly. Coverage
# is opt-in via ``--cov=...`` so individual ``pytest tests/foo.py``
# invocations stay fast.
addopts = "--tb=short"

[tool.coverage.run]
branch = true
source = ["src/autonoma"]
omit = [
    # Heavy ML stacks that only load when their hardware extra is
    # installed — counting them inflates the "uncovered" number in dev
    # environments that intentionally skip them.
    "src/autonoma/tts_omnivoice.py",
    "src/autonoma/tts_vibevoice.py",
    "src/autonoma/voice/asr.py",
]

[tool.coverage.report]
# Skip lines that won't be exercised by unit tests anyway. ``# pragma:
# no cover`` is honoured by default.
exclude_also = [
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]
show_missing = true
skip_covered = false

[tool.pyright]
# Static type checker. Starts in ``basic`` mode so the existing
# 4000+ line modules don't drown the signal — tighten to ``standard``
# once the baseline is clean. Optional ML imports (torch/omnivoice/
# vibevoice/sentence-transformers) live behind extras, so missing-
# import errors there are expected.
include = ["src", "tests"]
exclude = [
    "**/.venv",
    "**/__pycache__",
    "**/node_modules",
    "**/.next",
    "output",
    "logs",
    ".hf-cache",
    "data",
]
pythonVersion = "3.12"
typeCheckingMode = "basic"
reportMissingImports = "warning"
reportMissingTypeStubs = false

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [".venv", "output", "logs", ".hf-cache", "data"]

[tool.ruff.format]
# Match the existing code's preference: double quotes, unix line
# endings, magic trailing-comma respect (so list/tuple wrap is stable).
quote-style = "double"
line-ending = "lf"
skip-magic-trailing-comma = false

[tool.ruff.lint]
# Conservative starter rule set — pyflakes (F) and pycodestyle errors (E)
# catch real bugs (unused vars, undefined names, syntax). Bug-bear (B)
# adds catches for common pitfalls without forcing style rewrites. Add
# more rule families later once the baseline is clean.
select = ["E", "F", "B", "I"]
# - E501 (line too long): cosmetic, formatter's job.
# - B008 (function call in default arg): FastAPI/typer Depends() pattern.
# - B904 (raise without from): widespread pattern in this codebase;
#   adding ``from`` everywhere is its own follow-up.
# - B905 (zip strict): same — broad cleanup deferred.
# - B007 (unused loop var): often intentional iteration counters.
# - F841 (unused local): often diagnostic-only references kept for repl.
ignore = ["E501", "E402", "B008", "B904", "B905", "B007", "F841"]

[tool.ruff.lint.per-file-ignores]
# __init__.py files re-export symbols intentionally — they are the
# package's public surface, not unused imports.
"src/autonoma/**/__init__.py" = ["F401"]
# Tests routinely use ``assert`` and rely on fixtures being shadowed.
"tests/**" = ["B011"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.8.0",
    "pyright>=1.1.380",
    "pre-commit>=3.7.0",
]

# Pin torch / torchaudio to the CPU-only wheels on Linux x86_64 so the
# container image stays ~300 MB instead of the ~2.5 GB CUDA build pulled
# from PyPI by default. macOS and arm64 (Apple Silicon) resolve normally
# so dev machines keep MPS acceleration.
[tool.uv.sources]
torch = [
    { index = "pytorch-cpu", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]
torchaudio = [
    { index = "pytorch-cpu", marker = "sys_platform == 'linux' and platform_machine == 'x86_64'" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true
